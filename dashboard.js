/*
  DOCUMENTAÇÃO DO JAVASCRIPT (dashboard.js)

  Este ficheiro vai controlar o comportamento da tua tela de dashboard.
  Por agora, ele está a ser carregado, mas não executa nenhuma ação.

  No futuro, podes adicionar aqui:
  - Lógica para os cliques nos botões da barra de navegação.
  - Lógica para o que acontece ao clicar na barra de pesquisa.
  - Lógica para o que acontece ao clicar no botão de filtro.
*/

// Este evento espera que o HTML esteja completamente carregado
document.addEventListener('DOMContentLoaded', () => {

    console.log("Dashboard carregado e pronto!");

    // --- Exemplo de como adicionar eventos no futuro ---

    /*
    // Pega o botão de filtro pelo ID que definimos no HTML
    const filterButton = document.getElementById('filter-button');

    // Adiciona um "ouvinte" de clique
    if (filterButton) {
        filterButton.addEventListener('click', () => {
            alert("O botão de FILTRO foi clicado!");
            // Aqui podes abrir um menu de filtros, por exemplo.
        });
    }

    // Pega a barra de pesquisa pelo ID
    const searchButton = document.getElementById('search-button');

    if (searchButton) {
        searchButton.addEventListener('click', () => {
            alert("A barra de PESQUISA foi clicada!");
            // Aqui podes abrir uma tela de pesquisa, por exemplo.
        });
    }
    */

    // Podes "descomentar" (apagar os /* e */) o código acima
    // para testar os alertas de clique.

});